import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

interface <%= namePascal %>ScreenProps {
  navigation?: any;
  route?: any;
}

export function <%= namePascal %>Screen({ navigation, route }: <%= namePascal %>ScreenProps) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}><%= namePascal %></Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff',
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});
